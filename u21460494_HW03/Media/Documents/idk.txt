//define global arrays
var moduleArray = [];
var modulelecturerArray = [];

var tasknameArray = [];
var taskdateArray = [];

var descriptionArray = [];

var indexID = 0;

//add event listener
document.getElementById("Add").addEventListener("click", displayInputInfo);


//define function 
function displayInputInfo () {

    // unique id
    var UniqueID1 = Date.now();

    modulecode = document.getElementById("modulecode").value;
    document.getElementById("modulecode").textContent = modulecode;
    modulelecturer = document.getElementById("modulelecturer").value;
    document.getElementById("modulelecturer").textContent = modulelecturer;

        //error checking
        if (!modulecode || !modulelecturer)
        {
            alert ("Please enter the relevant information");
            return;
        }
        modulecodeArray.push({ modulecode: modulecode, modulelecturer: modulelecturer})
        //update arrays
        modulecodeArray.modulecode = modulecode;
        modulecodeArray.modulelecturer = modulelecturer;
        
        //update into the arrays
        //modulecodeArray[indexID] = modulecode;
       // modulelecturerArray[indexID] = modulelecturer;
       

        //add options to drop downmenu
        var dropdowncode =document.getElementById("dropcode");
        dropdowncode.options[dropdowncode.options.length] = new Option(modulecode);

        //to insert rows
        row = 1;
        var table1 = document.getElementById("tab1");
        var newrow = table1.insertRow(row);
        var cell1 = newrow.insertCell(0);
        var cell2 = newrow.insertCell(1);


        cell1.innerHTML = modulecodeArray.modulecode;
        cell2.innerHTML = modulecodeArray.modulelecturer;
        //cell1.innerHTML = modulecodeArray[indexID];
        //cell2.innerHTML = modulelecturerArray[indexID];
    
        row++;
        indexID ++;
}

//define function 

//add event listener
document.getElementById("Add").addEventListener("click", displayInputTask);

function displayInputTask () {

    // unique id
    var UniqueID2 = Date.now();
    taskname = document.getElementById("taskname").value;
    document.getElementById("taskname").textContent = taskname;
    //Due date
    var taskdate = document.getElementById("taskdate").value ;
    document.getElementById("taskdate").textContent = taskdate;


        //error checking
        if (!taskname || !taskdate)
        {
            alert ("Please enter the relevant information");
            return;
        }


         //update into the arrays
         tasknameArray[indexID] = taskname;
         taskdateArray[indexID] = taskdate;
         
 
         //add options to drop downmenu
         var dropdowntask = document.getElementById("droptask");
         dropdowntask.options[dropdowntask.options.length] = new Option(taskname);


        //to insert rows
        row = 1;
        var table2 = document.getElementById("tab2");
        var newrow = table2.insertRow(row);
        var cell1 = newrow.insertCell(0);
        var cell2 = newrow.insertCell(1);


        cell1.innerHTML = tasknameArray[indexID];
        cell2.innerHTML = taskdateArray[indexID];
        
        row++;
        indexID ++;
}

//add event listener
document.getElementById("Add").addEventListener("click", displayInputToDo);

//define function 
function displayInputToDo() {
    //get unique id
    description= document.getElementById("description").value;
    document.getElementById("description").textContent = description;
   

        //error checking
        if (!description)
        {
            alert ("Please enter the relevant information");
            return;
        }

        
        //to insert rows
        rows = 1;
        var table3 = document.getElementById("tab3");
        var newrow = table3.insertRow(row);
        var cell1 = newrow.insertCell(0);
        var cell2 = newrow.insertCell(1);
        var cell3 = newrow.insertCell(2);
        var cell4 = newrow.insertCell(3);


        cell1.innerHTML = modulecode;
        cell2.innerHTML = taskname;
        cell3.innerHTML = description;
        cell4.innerHTML = taskdate;
        
        rows++;
        indexID ++;

        //to delete a row
            /*function deleteRow() {

                    for (var i = 0; i < table3.row.length; i++) {
                            document.getElementById("tab3").deleteRow(onclick);
                        }
            }
            */
        
}
